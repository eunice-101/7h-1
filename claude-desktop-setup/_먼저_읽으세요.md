# 교수님이 직접 하셔야 하는 일 (이것만 하면 됩니다)

아래 5가지는 보안·계정 때문에 사람만 할 수 있습니다. 나머지 설정 파일은 모두 완성되어 있습니다.

## 1. Claude 요금제 (최고 성능의 핵심)
- Claude **Max 요금제** 가입 권장. 가장 똑똑한 모델(Opus)을 가장 많이 쓸 수 있습니다.
- 가입: claude.ai → 우측 상단 Upgrade → Max 선택.
- 이유: 무료·하위 요금제는 사용량 한도가 낮아 긴 집필·검증 작업이 중간에 끊깁니다.

## 2. Claude Code 설치 (Windows)
- 방법 A (쉬움): claude.ai/code 접속 → Windows용 데스크톱 앱 내려받아 설치.
- 방법 B (CLI): 자세한 절차는 `설치안내_Windows.md` 참고.

## 3. 로그인
- 설치 후 첫 실행 시 1번 가입한 Claude 계정으로 로그인.

## 4. 설정 파일 복사 (복사 위치는 설치안내에 그림처럼 정리)
- 이 묶음의 `CLAUDE.md`와 `agents` 폴더를 `C:\Users\[사용자이름]\.claude\` 안에 넣기.

## 5. 외부 연동 로그인 (선택, 디자인·문서 작업 시)
- **Canva**: PPT·디자인 자동 생성용. 연동 화면에서 Canva 계정 1회 로그인.
- **Google Drive**: 드라이브 문서 읽기·작성용. 1회 로그인.
- 이 로그인은 보안상 사람이 직접 해야 하며, 클릭 몇 번이면 끝납니다.

---

# 제(비서)가 이미 다 해놓은 것
- 인격 지침(CLAUDE.md) 완성
- 역할 에이전트 4종(cso·worker·reviewer 2인) 완성
- 분야 모듈 6종(학술·집필·PPT·앱·데이터·일반) 통합
- 설치·연동 안내서 작성
- 기능별 최고 성능 사용법 정리

교수님은 위 1~5번만 하시면, 새 대화창을 열 때마다 이 수준의 비서가 자동으로 작동합니다.
